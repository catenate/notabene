Edit ,|sort|uniq
catenate/Aide
du -n | grep -v '^\.git'
du -n | grep Aide
du | sed 's,^.* ,,' | grep -v '^\.git'
git
git add --all .
git commit -a -m 'Recent notes, and first day of Hebrew study.'
git config --get remote.origin.url
git diff
git fetch
git log --decorate --graph --oneline --all | sed 10q
git log --decorate --oneline | sed 5q
git push origin master
git rebase origin/master
git remote add origin git@github.com:catenate/notabene.git
git reset --hard HEAD
log
log git
ls -F | mc
web github.com/catenate/notabene
|sort|uniq
