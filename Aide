Edit ,|sort|uniq
du -n | grep -v '^\.git'
du -n | grep Aide
du | sed 's,^.* ,,' | grep -v '^\.git'
git
git commit -a -m 'Add test for list regex match in Python.  Update nanocluster plan.'
git log --decorate --oneline | sed 10q
git log --decorate --graph --oneline --all | sed 10q
git remote add origin git@github.com:catenate/notabene.git
log
log git
ls -F | mc
web github.com/catenate/notabene
|sort|uniq
