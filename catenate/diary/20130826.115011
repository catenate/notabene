nb linux open smtp port 25 state service nmap localhost inet interface yum install mailx service postfix restart config

host sudo yum -y install postfix
host sudo service postfix restart
host sudo nmap -sS -O 127.0.0.1
# 25/tcp   open  smtp

host sudo yum -y install mailx
mail -v -s test -a /etc/postfix/main.cf $mailto
# inet_interfaces = localhost
